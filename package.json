{
  "name": "relay-email-scheduler",
  "version": "1.0.0",
  "description": "Alternate-day email schedule runner — sends queued batches from email-schedule.csv through a transactional email provider.",
  "main": "backend-server.js",
  "scripts": {
    "start": "node backend-server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "csv-parse": "^5.5.6",
    "nodemailer": "^6.9.14",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
